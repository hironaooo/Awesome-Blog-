<% if flash.any? %>
    <% flash.each do |type,msg| %>  <!-- flashはhashに入っている、たとえ１つだとしても -->
        <div class="alert alert-<%= type %>">
            <div><%= msg %></div>
        </div>
    <% end %>
<% end %>